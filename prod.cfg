[buildout]
extends =
  base.cfg

parts +=
  zeoserver

[instance]
zeo-address = zeo:${port:zeo}
zeo-client = on
shared-blob = on
zeo-client-cache-size = 128MB
zodb-cache-size = 50000
blob-storage = ${buildout:directory}/var/blobcache
